// Copy this file to `config.local.js` and paste your real Discord webhook URL in.
// `config.local.js` is gitignored — the secret stays on this machine only.
//
// Where to get it: Discord → Server Settings → Integrations → Webhooks
//                  → New Webhook → Copy Webhook URL
export const DISCORD_WEBHOOK_URL = '';
