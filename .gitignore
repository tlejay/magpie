# Secrets — never commit
src/config.local.js
*.local.js
.env
.env.*
!.env.example

# macOS
.DS_Store

# Editors
.vscode/
.idea/

# Scratch
_tmp/
*.log
# impeccable: share the ignore config, keep the cache and personal overrides out
.impeccable/hook.cache.json
.impeccable/config.local.json
